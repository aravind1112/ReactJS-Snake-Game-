import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.retrosnakegame.app',
  appName: 'Retro snake game',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
